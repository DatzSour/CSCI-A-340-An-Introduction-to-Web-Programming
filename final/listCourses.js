var listArea = document.getElementById('list');

        var content = list.Area.innerHTML

        object.HTML.forEach(function (element, index) {
            content = cotnet + element;
        });

        listArea.innerHTML = content;